const express=require('express');const multer=require('multer');const {verificarToken,soloAdmin}=require('../middleware/auth');const C=require('../controllers/nexoIa.controller');const r=express.Router();const upload=multer({storage:multer.memoryStorage(),limits:{fileSize:10*1024*1024,files:1}});r.use(verificarToken);r.get('/',C.listar);r.get('/:empresa/config',soloAdmin,C.obtenerConfig);r.put('/:empresa/config',soloAdmin,C.guardarConfig);r.post('/:empresa/documentos',soloAdmin,upload.single('documento'),C.subirDocumento);r.post('/sugerencias/:id/feedback',C.feedback);r.get('/:empresa/salud',C.salud);r.get('/:empresa/:id',C.detalle);r.post('/:empresa/:id/generar',C.generar);module.exports=r;
